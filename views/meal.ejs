<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Meal</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <link rel="stylesheet" href="../css/headers.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@docsearch/css@3">
    <link rel="stylesheet" href="../css/bootstrap.min.css">
    <link rel="stylesheet" href="../css/sign-in.css"> 
    <style>
        body {
            background-color: #f2e7f3; /* Light purple background */
        }
        .text-bg-dark {
            color: #6c757d !important; /* Dark text color */
        }
        .btn-primary {
            background-color: #9c27b0;
            border-color: #9c27b0; /* Purple button */
        }
        .btn-primary:hover {
            background-color: #6a1b9a;
            border-color: #6a1b9a; /* Hover color change */
        }
        .card {
            background-color: #ffffff;
            border: 1px solid #dee2e6;
            border-radius: .25rem;
        }
        .card-header {
            background-color: #9c27b0;
            color: #ffffff;
        }
    </style> 
</head>

<body>
    <header class="p-3 text-bg-dark">
        <div class="container">
            <div class="d-flex flex-wrap align-items-center justify-content-center justify-content-lg-start">

            </div>
        </div>
    </header>
    <div class="container mt-5">
        <h2 class="text-center" id="cool">Weather App</h2>
        <form action="/recipe" method="get" class="form-signin w-100 m-auto mt-3"> 
            <input type="text" id="cityInput" name="mealName" placeholder="Enter city name" />
            <button type="submit" class="btn btn-primary w-40 py-2">Get Weather</button>
        </form>

        <% if (error) { %>
            <div class="alert alert-danger mt-3" role="alert">
                <%= error %>
            </div>
        <% } else { %>
        
            <div id="weather-card" class="card mt-5 m-auto text-center">
                <div class="card-header">
                    Weather Information
                </div>

                <div class="card-body">
                    <div id="weather-info" class="row g-3">
                        <div class="col-md-4">
                            <div class="d-flex align-items-center">
                                <i class="fas fa-thermometer-half"></i>
                                <p id="meal">Meal: <%= mealData ? mealData.strMeal : '' %></p>
                            </div>
                        </div>
                        <div class="col-md-4">
                            <div class="d-flex align-items-center">
                                <i class="fas fa-cloud"></i>
                                <p id="category">Category: <%= mealData ? mealData.strCategory : '' %></p>
                            </div>
                        </div>
                        <div class="col-md-4">
                            <div class="d-flex align-items-center">
                                <i class="fas fa-cloud"></i>
                                <p id="area">Area: <%= mealData ? mealData.strArea : '' %></p>
                            </div>
                        </div>

                        <div class="col-md-12 mt-3">
                            <h4>Ingredients:</h4>
                            <ul>
                                <% mealData.strIngredients.forEach(ingredient => { %>
                                    <li><%= ingredient %></li>
                                <% }); %>
                            </ul>
                        </div>
                        
                        
        
                    </div>
                </div>

            </div>
        <% } %>



    </div>

    <script>
        window.history.replaceState({}, document.title, window.location.pathname);
    </script>
</body>